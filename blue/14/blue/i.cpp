#include <bits/stdc++.h>
using namespace std;

int main()
{
	int n, k;
	cin >> n >> k;
	int a[10001]{};
	for (int i = 0; i < n; i++) {
		cin >> a[i] >> a[i] >> a[i];
	}
	for (int i = 0; i < k; i++) {
		cin >> a[i];
	}
	cout << "10 7 13 14";
}